{
  "name": "twitter-marketing-iftt",
  "version": "1.0.0",
  "description": "Twitter marketing automation with IFTTT and Airtable",
  "main": "dist/rtCleaner.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/rtCleaner.js",
    "dev": "ts-node src/rtCleaner.ts",
    "clean": "rm -rf dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "airtable": "^0.12.2",
    "dotenv": "^16.3.1",
    "node-cron": "^3.0.3",
    "node-telegram-bot-api": "^0.66.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/node-cron": "^3.0.11",
    "@types/node-telegram-bot-api": "^0.64.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0"
  },
  "author": "",
  "license": "MIT"
}